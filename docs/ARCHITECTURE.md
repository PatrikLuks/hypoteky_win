# Nasazovací Architektura

## 🏗️ Production Infrastructure

```
┌─────────────────────────────────────────────────────────────────┐
│                         INTERNET / USERS                         │
└────────────────────────────────────┬────────────────────────────┘
                                     │ HTTPS:443
                                     ▼
                      ┌──────────────────────────┐
                      │   NGINX Reverse Proxy    │
                      │  (Gzip, SSL, Security)   │
                      └────────────┬─────────────┘
                                   │
                ┌──────────────────┼──────────────────┐
                │                  │                  │
                ▼                  ▼                  ▼
        ┌──────────────┐  ┌────────────────┐  ┌─────────────┐
        │  Static /    │  │  Gunicorn      │  │   Media /   │
        │  (CSS,JS)    │  │  Application   │  │   Uploads   │
        │ /static/     │  │ (4 workers)    │  │ /media/     │
        └──────────────┘  └────────┬───────┘  └─────────────┘
                                   │
                                   │ Django ORM
                                   ▼
                      ┌──────────────────────────┐
                      │    MySQL Database        │
                      │  (UTF-8MB4, InnoDB)      │
                      │  - Klient (56 polí)      │
                      │  - Zmena (audit)         │
                      │  - NotifikaceLog         │
                      │  - User, Profile, etc.   │
                      └──────────────────────────┘
```

## 📊 Application Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                   DJANGO APPLICATION                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐   │
│  │           VIEWS & ENDPOINTS                            │   │
│  │  ├─ /klienti/ (CRUD - Create, Read, Update, Delete)   │   │
│  │  ├─ /dashboard/ (Analytics & Workflow Progress)       │   │
│  │  ├─ /reporting/ (Statistics & Reports)                │   │
│  │  ├─ /api/klienti/ (REST API - JWT Auth)              │   │
│  │  └─ /admin/ (Django Admin Panel)                      │   │
│  └────────────────────────────────────────────────────────┘   │
│                          │                                      │
│                          ▼                                      │
│  ┌────────────────────────────────────────────────────────┐   │
│  │           BUSINESS LOGIC & UTILS                       │   │
│  │  ├─ Workflow Engine (15 steps)                         │   │
│  │  ├─ Encryption (Fernet - django-encrypted-model-fields) │ │
│  │  ├─ Email Notifications (SMTP)                        │   │
│  │  ├─ Import/Export (CSV, XLSX)                         │   │
│  │  └─ Audit Logging (Zmena model)                       │   │
│  └────────────────────────────────────────────────────────┘   │
│                          │                                      │
│                          ▼                                      │
│  ┌────────────────────────────────────────────────────────┐   │
│  │              MODELS & DATABASE                         │   │
│  │  ├─ Klient (56 fields, 14 encrypted)                 │   │
│  │  ├─ HypotekaWorkflow (15 steps)                       │   │
│  │  ├─ Zmena (Audit Log)                                 │   │
│  │  ├─ NotifikaceLog (Email Tracking)                    │   │
│  │  ├─ User & UserProfile (RBAC)                         │   │
│  │  └─ Attachment, File, etc.                            │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow - New Client Workflow

```
┌─────────────┐
│  User/Form  │
└──────┬──────┘
       │ POST /klienti/create/
       ▼
┌─────────────────────────────────────────┐
│   Form Validation & Sanitization        │
│   (XSS protection, CSRF token check)    │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│   Klient Model.save()                   │
│   ├─ Encrypt sensitive fields (14)      │
│   ├─ Create audit log entry (Zmena)     │
│   ├─ Trigger notification hooks         │
│   └─ Update user/profile                │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│   MySQL Database                        │
│   ├─ INSERT into klienti                │
│   ├─ INSERT into zmena (audit)          │
│   └─ UPDATE user_profile                │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│   Email Notification (Async)            │
│   ├─ Send to poradce (advisor)          │
│   ├─ Send to klient (if email set)      │
│   └─ Log in NotifikaceLog               │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│   Response to User                      │
│   ├─ Redirect to /klienti/detail/       │
│   └─ Show success message               │
└─────────────────────────────────────────┘
```

## 📅 Cron/Scheduling

```
┌─────────────────────────────────────────────────────┐
│          CRON JOBS (via systemd/crontab)            │
├─────────────────────────────────────────────────────┤
│                                                      │
│  EVERY DAY @ 08:00                                  │
│  ▼                                                   │
│  python manage.py send_deadline_notifications      │
│  ├─ Find Klient where deadline ≤ 3 days            │
│  ├─ Send email to poradce + klient                 │
│  └─ Log to NotifikaceLog                           │
│                                                      │
│  EVERY MONDAY @ 09:00                               │
│  ▼                                                   │
│  python manage.py send_reporting_email             │
│  ├─ Generate weekly statistics                     │
│  ├─ Send to poradce + staff/superusers             │
│  └─ Log to NotifikaceLog                           │
│                                                      │
└─────────────────────────────────────────────────────┘
```

## 🔐 Security Layers

```
┌─────────────────────────────────────────────────────────┐
│                   SECURITY STACK                        │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Layer 1: Network Security                             │
│  ├─ HTTPS/TLS 1.2+ (Let's Encrypt)                     │
│  ├─ Nginx security headers                            │
│  └─ Firewall (iptables/ufw)                           │
│                                                          │
│  Layer 2: Application Security                         │
│  ├─ CSRF token validation                             │
│  ├─ XSS protection (autoescaping)                     │
│  ├─ SQL injection prevention (ORM)                    │
│  ├─ Rate limiting (optional)                          │
│  └─ Security headers (X-Frame-Options, etc.)          │
│                                                          │
│  Layer 3: Data Security                                │
│  ├─ Password hashing (PBKDF2)                         │
│  ├─ Field encryption (Fernet) for 14 fields          │
│  ├─ Secure cookies (HttpOnly, Secure flag)           │
│  └─ JWT token signing                                 │
│                                                          │
│  Layer 4: Authentication & Authorization               │
│  ├─ Django auth + JWT tokens                          │
│  ├─ Role-Based Access Control (RBAC)                  │
│  │  ├─ poradce (advisor) - can see all clients        │
│  │  └─ klient (client) - can see only own data        │
│  ├─ OTP middleware (optional 2FA)                     │
│  └─ API permission classes                            │
│                                                          │
│  Layer 5: Audit & Compliance                           │
│  ├─ Zmena model (audit logs)                          │
│  ├─ NotifikaceLog (email tracking)                    │
│  ├─ Django admin logs                                 │
│  └─ GDPR compliance (export/delete endpoints)         │
│                                                          │
│  Layer 6: Monitoring & Response                        │
│  ├─ Sentry (error monitoring)                         │
│  ├─ Server logs (tail -f)                            │
│  ├─ Application metrics                               │
│  └─ Incident response plan                            │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

## 📈 Scaling Considerations

```
Current Setup (Production Ready):
┌─────────────────────────────────────────────────────┐
│  Single Server (4 cores, 8GB RAM)                   │
│  ├─ Nginx (1 process)                              │
│  ├─ Gunicorn (4 workers)                           │
│  ├─ MySQL (local or remote)                        │
│  └─ Redis (optional, for caching)                  │
│                                                      │
│  Expected Capacity:                                 │
│  ├─ 100-500 concurrent users                       │
│  ├─ 10,000-50,000 requests/day                     │
│  └─ 50MB-500MB database size                       │
└─────────────────────────────────────────────────────┘

Scaling Strategy (Future):
├─ Load Balancer (HAProxy/AWS ELB)
├─ Multiple Django Servers (2-4)
├─ Separate MySQL Server
├─ Redis Cluster (caching)
├─ S3 for static files & uploads
├─ CloudFront for CDN
└─ Elasticsearch for logging
```

## 🚨 Disaster Recovery

```
┌──────────────────────────────────────────────────────┐
│           BACKUP & RECOVERY PLAN                     │
├──────────────────────────────────────────────────────┤
│                                                       │
│  MySQL Database:                                     │
│  ├─ Daily backup: /backups/hypoteky_*.sql           │
│  ├─ Weekly archive to AWS S3                        │
│  ├─ Retention: 30 days                              │
│  └─ RTO: 1 hour, RPO: 1 day                         │
│                                                       │
│  Application Code:                                   │
│  ├─ Git repository (GitHub)                         │
│  ├─ Git tags (v1.0.0-prod)                          │
│  └─ Deployment history                              │
│                                                       │
│  Static Files:                                       │
│  ├─ Nginx /static/ directory                        │
│  ├─ Can be regenerated (collectstatic)              │
│  └─ CDN cache on CloudFront                         │
│                                                       │
│  Rollback Procedure:                                 │
│  1. Stop application: systemctl stop hypoteky       │
│  2. Revert code: git revert HEAD                    │
│  3. Restore DB: mysql < backup_*.sql                │
│  4. Restart: systemctl start hypoteky               │
│  5. Verify: curl https://example.com/               │
│                                                       │
└──────────────────────────────────────────────────────┘
```

## 📊 Performance Monitoring

```
┌──────────────────────────────────────────────────────┐
│       MONITORING & ALERTING DASHBOARD                │
├──────────────────────────────────────────────────────┤
│                                                       │
│  Key Metrics:                                        │
│  ├─ Response Time (< 2s)      [Nginx logs]          │
│  ├─ Error Rate (< 0.1%)       [Sentry]              │
│  ├─ CPU Usage (< 70%)         [top/htop]            │
│  ├─ Memory (< 80%)            [free]                │
│  ├─ Disk Space (< 80%)        [df]                  │
│  ├─ Database Size             [MySQL du]            │
│  ├─ Connection Pool Usage      [MySQL show status]   │
│  ├─ Uptime (> 99.9%)          [systemctl]           │
│  └─ Email Delivery Rate        [NotifikaceLog]      │
│                                                       │
│  Alerting:                                           │
│  ├─ Error rate > 1% → Pagerduty/Email              │
│  ├─ Response time > 5s → Warning                    │
│  ├─ CPU > 80% → Scale up                            │
│  ├─ Disk > 90% → Emergency cleanup                  │
│  └─ Uptime < 99% → Incident report                  │
│                                                       │
│  Tools:                                              │
│  ├─ Prometheus (metrics)                            │
│  ├─ Grafana (visualization)                         │
│  ├─ ELK Stack (logging)                             │
│  ├─ Sentry (error tracking)                         │
│  └─ NewRelic/DataDog (APM)                          │
│                                                       │
└──────────────────────────────────────────────────────┘
```

---

**Created**: 5. ledna 2026  
**For**: hypoteky_win (Django CRM)  
**Version**: 1.0 - Production Ready
